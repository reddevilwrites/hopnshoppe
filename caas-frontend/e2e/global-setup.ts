/**
 * Playwright global setup — runs once before all tests.
 *
 * Currently a no-op placeholder. Extend this if you need to:
 * - Pre-seed a persistent test database fixture
 * - Warm up the application
 * - Authenticate a shared session state
 */
export default async function globalSetup() {
  // No-op
}
